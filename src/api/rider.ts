import { ApiResponse, axios_server } from ".";

// export async function getRiderProfile({ token }: { token: string }) {
//   const res = await axios_server.get<ApiResponse<any>>("/rider/profile", {
//     headers: {
//       Authorization: `Bearer ${token}`,
//     },
//   });
// }
